import 'package:flutter/material.dart';

class AppColors {
  // Suppress instantiation with a proper private constructor
  AppColors._();

  static const Color primary = Color(0xFF0F4C81);     // Liturgical Blue
  static const Color secondary = Color(0xFFC8A951);   // Liturgical Gold
  static const Color background = Color(0xFFFAFAFA);  // Anti-glare Background
  static const Color surface = Color(0xFFFFFFFF);     // Card & Tab Container
  static const Color textDark = Color(0xFF14191F);    // High-contrast Deep Slate
}